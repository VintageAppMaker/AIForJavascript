// 함수 add(num1, num2) 정의
function add(num1, num2) {
  // 결과값 = num1 + num2
  var result = num1 + num2;
  // 결과값을 리턴
  return result;
}

// 시작
// add 함수에 3과 5를 입력하여 결과값을 받음
var result = add(3, 5);
// 결과값 출력
console.log(result);
